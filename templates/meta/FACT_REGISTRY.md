# 事实注册表（禁止凭记忆陈述）

Agent 在代码、docs、回复中涉及下列事实时，**必须先读取对应源文件**。源文件冲突时按 `AGENTS.md` 层级裁决。

**本表是索引，不是替代源文件。**

---

## 0. Harness 文档约定

### 0.1 `[TBD: filled by Fxx]` 标记

- **含义**：该段落由工单 Fxx 在阶段 2 实现、阶段 3 同步 docs；**不是**「内容缺失」。
- **阶段 1**：必须阅读 TBD 章节内已有表格/字段/示例，不得因标题跳过。
- **阶段 3**：实现后更新对应 doc，将 TBD 改为 `[filled by Fxx]` 或删除标记。

---

## 1. 源文件索引

> 由 harness-specify 根据 docs/specs/ 和 docs/meta/ 自动生成。
> 每行定义哪类事实的权威来源在哪份文件。

| 事实类别 | 权威源文件 |
|----------|------------|
| API 路径、请求/响应字段 | `docs/specs/{{domain}}/spec.md` |
| 错误码、HTTP 状态 | `ARCHITECTURE.md` 错误码章节 |
| 分层、依赖方向、目录结构 | `ARCHITECTURE.md` |
| 数据模型、表结构 | `docs/specs/{{domain}}/spec.md` |
| 部署架构、服务编排、Dockerfile | `docs/specs/_architecture/DEPLOYMENT.md` |
| 环境变量、运行配置 | `docs/specs/_architecture/DEPLOYMENT.md` 环境变量段 |

---

## 2. 高频易错事实（必须以文件为准）

> 下列为**常见考点**，具体取值、边界、例外以源文件为准；勿把本表当最终契约。
> 由 harness-specify 在生成规格时填充。

### 2.1 全局

| 项 | 核验要点 |
|----|----------|
| API 前缀 | {{从 project.yaml context 提取}} |
| 追踪字段 | {{列出贯穿全链路的追踪字段}} |
| 配置优先级 | {{列出配置加载优先级}} |

### 2.2 关键接口

| 项 | 核验要点 |
|----|----------|
| {{接口名称}} | {{关键约束和边界条件}} |

---

## 3. 变更流程

1. 先改权威源文件（通常 `docs/specs/` 下对应 spec.md）。
2. 再改本表索引（若仍适用）。
3. 同步 `ARCHITECTURE.md`（若影响架构决策）。
4. 若影响实现，新建变更（走 harness-change 流程）。
5. 在 `progress.md` 记录「事实变更」摘要。

---

## 4. Agent 自检

输出涉及上表任一项前，自问：

- [ ] 我是否已读取对应源文件？
- [ ] 约束摘要是否引用了实际读到的内容？
- [ ] 若与记忆不符，是否标记「事实修正」？
